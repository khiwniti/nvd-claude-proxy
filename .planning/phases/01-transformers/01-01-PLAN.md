---
phase: 01-transformers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, src/nvd_claude_proxy/transformers/base.py, src/nvd_claude_proxy/transformers/chain.py]
autonomous: true
requirements: [TRANS-01, TRANS-07]

must_haves:
  truths:
    - "New dependencies `json-repair` and `json5` are added to `pyproject.toml`"
    - "BaseTransformer interface exists and defines request/response/stream methods"
    - "TransformerChain can manage a sequence of transformers and apply them"
  artifacts:
    - path: "src/nvd_claude_proxy/transformers/base.py"
      provides: "BaseTransformer abstract class"
    - path: "src/nvd_claude_proxy/transformers/chain.py"
      provides: "TransformerChain implementation"
  key_links:
    - from: "TransformerChain"
      to: "BaseTransformer"
      via: "List of transformers"
---

<objective>
Set up the core infrastructure for the Modular Transformer Pipeline and update project dependencies.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dependencies in pyproject.toml</name>
  <files>nvd-claude-proxy/pyproject.toml</files>
  <action>
    Add `json-repair>=0.30.0` and `json5>=0.9.28` to the `dependencies` list in `pyproject.toml`.
  </action>
  <verify>
    <automated>grep -E "json-repair|json5" nvd-claude-proxy/pyproject.toml</automated>
  </verify>
  <done>Dependencies added to pyproject.toml</done>
</task>

<task type="auto">
  <name>Task 2: Implement BaseTransformer</name>
  <files>nvd-claude-proxy/src/nvd_claude_proxy/transformers/base.py</files>
  <action>
    Create `src/nvd_claude_proxy/transformers/base.py` with an abstract base class `BaseTransformer`.
    Include methods:
    - `transform_request(self, payload: dict) -> dict`
    - `transform_response(self, response: dict) -> dict`
    - `transform_stream_chunk(self, chunk: dict) -> dict | None`
    Use `abc.ABC` and `abc.abstractmethod`. Provide default implementations (passthrough) where appropriate to make it easier to subclass.
  </action>
  <verify>
    <automated>python3 -c "from nvd_claude_proxy.transformers.base import BaseTransformer" 2>/dev/null || echo "Failed"</automated>
  </verify>
  <done>BaseTransformer interface created</done>
</task>

<task type="auto">
  <name>Task 3: Implement TransformerChain</name>
  <files>nvd-claude-proxy/src/nvd_claude_proxy/transformers/chain.py</files>
  <action>
    Create `src/nvd_claude_proxy/transformers/chain.py` with `TransformerChain`.
    It should take a list of `BaseTransformer` instances and have methods to apply them sequentially:
    - `transform_request(self, payload: dict) -> dict`
    - `transform_response(self, response: dict) -> dict`
    - `transform_stream_chunk(self, chunk: dict) -> dict | None`
    If a transformer returns `None` during streaming, it should stop the chain for that chunk.
  </action>
  <verify>
    <automated>python3 -c "from nvd_claude_proxy.transformers.chain import TransformerChain" 2>/dev/null || echo "Failed"</automated>
  </verify>
  <done>TransformerChain implemented</done>
</task>

</tasks>

<verification>
Check that the new files exist and can be imported.
</verification>

<success_criteria>
- `pyproject.toml` updated.
- `BaseTransformer` and `TransformerChain` implemented and importable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-transformers/01-01-SUMMARY.md`
</output>
