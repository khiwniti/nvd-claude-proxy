---
phase: 02-dashboard-session
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified: [src/nvd_claude_proxy/cli/main.py, src/nvd_claude_proxy/app.py, dashboard/src/components/LiveStream.tsx, dashboard/src/components/UsageGraphs.tsx]
autonomous: true
requirements: [DASH-05, SESS-03]

must_haves:
  truths:
    - "CLI supports --dashboard flag to launch the server"
    - "Live stream visualization displays both Anthropic and OpenAI protocols"
    - "Usage graphs are visible in the dashboard"
  artifacts:
    - path: "dashboard/src/components/LiveStream.tsx"
      provides: "Live stream dual-window visualization"
    - path: "src/nvd_claude_proxy/cli/main.py"
      provides: "Updated CLI for dashboard support"
---

<objective>
Implement live stream visualization, usage graphs, and update the CLI to support launching the dashboard.

Purpose: Complete the web dashboard and deployment story.
Output: Finalized dashboard with visualization features and integrated CLI support.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@nvd-claude-proxy/.planning/phases/02-dashboard-session/02-04-SUMMARY.md
@nvd-claude-proxy/src/nvd_claude_proxy/cli/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Live Stream Visualization</name>
  <files>dashboard/src/components/LiveStream.tsx, src/nvd_claude_proxy/app.py</files>
  <action>
    - Set up a WebSocket or Server-Sent Events (SSE) endpoint in `app.py` for live request/response data.
    - Implement a `LiveStream` component with dual windows:
      - Left: Incoming Anthropic Protocol events.
      - Right: Outgoing/Translated OpenAI-style events.
    - Use WebSockets to stream events from the proxy routes to the dashboard.
  </action>
  <verify>
    <automated>cd dashboard && npm run lint</automated>
  </verify>
  <done>Live stream visualization displays real-time translations.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Usage Graphs</name>
  <files>dashboard/src/components/UsageGraphs.tsx</files>
  <action>
    - Use `recharts` to display token usage, latency, and request counts.
    - Fetch data from existing prometheus metrics or new dashboard API stats.
  </action>
  <verify>
    <automated>cd dashboard && npm run lint</automated>
  </verify>
  <done>Usage graphs provide insights into proxy performance.</done>
</task>

<task type="auto">
  <name>Task 3: CLI & Serving Integration</name>
  <files>src/nvd_claude_proxy/cli/main.py, src/nvd_claude_proxy/app.py</files>
  <action>
    - Add `--dashboard` and `--dashboard-port` arguments to `ncp` CLI.
    - Update `app.py` to serve the `dashboard/dist/` directory as static files using `StaticFiles`.
    - Update the `Makefile` with a `make build-dashboard` command.
  </action>
  <verify>
    <automated>ncp --help | grep dashboard</automated>
  </verify>
  <done>CLI correctly handles dashboard launch and static file serving.</done>
</task>

</tasks>

<success_criteria>
- Dashboard is accessible via CLI command and serves built React app.
- Real-time streaming is visualized in dual windows.
- Usage graphs show meaningful data.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-session/02-05-SUMMARY.md`
</output>
