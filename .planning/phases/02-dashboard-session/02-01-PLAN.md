---
phase: 02-dashboard-session
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/DESIGN.md, src/nvd_claude_proxy/db/models.py, src/nvd_claude_proxy/db/database.py]
autonomous: true
requirements: [SESS-01, DASH-01]

must_haves:
  truths:
    - "Documentation exists describing session persistence architecture"
    - "SQLite database can be initialized with required tables"
  artifacts:
    - path: "docs/DESIGN.md"
      provides: "Project design documentation"
    - path: "src/nvd_claude_proxy/db/models.py"
      provides: "SQLAlchemy models for sessions and config"
    - path: "src/nvd_claude_proxy/db/database.py"
      provides: "Database connection and session management"
---

<objective>
Document the system design and implement the persistent storage layer using SQLite and SQLAlchemy.

Purpose: Provide a foundation for session persistence and dashboard configuration.
Output: Design documentation and database schema implementation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@nvd-claude-proxy/GEMINI.md
@nvd-claude-proxy/src/nvd_claude_proxy/translators/tool_translator.py
@nvd-claude-proxy/src/nvd_claude_proxy/translators/transformers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DESIGN.md</name>
  <files>nvd-claude-proxy/docs/DESIGN.md</files>
  <action>
    Create a comprehensive design document for the Persistent Session Manager and Web Dashboard.
    Include:
    - Architecture diagram/description: How middleware interacts with SQLite.
    - Database Schema:
      - `sessions`: id, api_key (sk-ncp-*), tool_id_map (JSON), created_at, updated_at.
      - `model_mappings`: id, anthropic_model, nvd_model, capability_overrides (JSON).
      - `transformer_toggles`: id, session_id or model_id, transformer_name, enabled (bool).
    - UI Design: Live stream visualization (Anthropic vs OpenAI protocol), Session management, and Usage graphs.
  </action>
  <verify>
    <automated>ls nvd-claude-proxy/docs/DESIGN.md</automated>
  </verify>
  <done>DESIGN.md is created with all required sections.</done>
</task>

<task type="auto">
  <name>Task 2: Implement DB Layer</name>
  <files>nvd-claude-proxy/src/nvd_claude_proxy/db/models.py, nvd-claude-proxy/src/nvd_claude_proxy/db/database.py</files>
  <action>
    - Add `sqlalchemy` and `aiosqlite` to `pyproject.toml` (via `pip install` or manual edit).
    - Create `database.py` to handle async engine and sessionmaker.
    - Create `models.py` with SQLAlchemy declarative base for:
      - `SessionRecord`: Persistent storage for ToolIdMap and metadata.
      - `ModelMapping`: Custom mappings for dashboard.
      - `TransformerToggle`: Configuration for enabling/disabling specific transformers.
  </action>
  <verify>
    <automated>python3 -c "from nvd_claude_proxy.db.models import Base; print('Models loaded')"</automated>
  </verify>
  <done>Database infrastructure is ready for integration.</done>
</task>

</tasks>

<success_criteria>
- DESIGN.md is comprehensive.
- DB models reflect the requirements.
- Async database engine is configured.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-session/02-01-SUMMARY.md`
</output>
