---
phase: 02-dashboard-session
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified: [dashboard/package.json, dashboard/src/App.tsx, dashboard/index.html]
autonomous: true
requirements: [DASH-02, DASH-03]

must_haves:
  truths:
    - "React application can be built and served"
    - "User can view and manage sessions and models in a browser"
  artifacts:
    - path: "dashboard/src/components/SessionList.tsx"
      provides: "Session management UI"
    - path: "dashboard/src/components/ModelMapping.tsx"
      provides: "Model mapping UI"
---

<objective>
Scaffold the React frontend for the dashboard and implement the management UI for sessions and model mappings.

Purpose: Provide a visual interface for managing proxy state.
Output: SPA frontend with management capabilities.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@nvd-claude-proxy/.planning/phases/02-dashboard-session/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + Tailwind</name>
  <files>dashboard/package.json, dashboard/index.html, dashboard/tailwind.config.js</files>
  <action>
    - Initialize a new Vite project in the `dashboard/` directory.
    - Install dependencies: `react`, `react-dom`, `tailwindcss`, `lucide-react`, `axios`, `recharts`.
    - Configure Tailwind CSS.
    - Set up a basic layout with dark mode support.
  </action>
  <verify>
    <automated>cd dashboard && npm run build</automated>
  </verify>
  <done>Frontend scaffolding is complete and builds successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Management UI</name>
  <files>dashboard/src/components/SessionList.tsx, dashboard/src/components/ModelMapping.tsx, dashboard/src/App.tsx</files>
  <action>
    - Create `SessionList` component to fetch and display sessions from `/api/dashboard/sessions`.
    - Create `ModelMapping` component to fetch and display model mappings from `/api/dashboard/models`.
    - Implement basic forms to add/edit/delete models and sessions.
    - Add dark mode toggle and navigation sidebar.
  </action>
  <verify>
    <automated>cd dashboard && npm run lint</automated>
  </verify>
  <done>Management UI is functional for basic CRUD operations.</done>
</task>

</tasks>

<success_criteria>
- Frontend builds and displays a functional management UI.
- Dark mode is implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-session/02-04-SUMMARY.md`
</output>
