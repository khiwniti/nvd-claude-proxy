---
phase: 02-dashboard-session
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [src/nvd_claude_proxy/routes/dashboard.py, src/nvd_claude_proxy/app.py]
autonomous: true
requirements: [DASH-01, DASH-04]

must_haves:
  truths:
    - "API endpoints exist for listing sessions and updating model mappings"
    - "Model configurations can be toggled via API"
  artifacts:
    - path: "src/nvd_claude_proxy/routes/dashboard.py"
      provides: "FastAPI router for dashboard management"
---

<objective>
Implement the backend API for the web dashboard, providing CRUD operations for sessions, model mappings, and configuration toggles.

Purpose: Enable interactive management and monitoring of the proxy.
Output: Dashboard API endpoints and integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@nvd-claude-proxy/.planning/phases/02-dashboard-session/02-02-SUMMARY.md
@nvd-claude-proxy/src/nvd_claude_proxy/db/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Session and Model CRUD Endpoints</name>
  <files>src/nvd_claude_proxy/routes/dashboard.py</files>
  <action>
    Implement the following endpoints:
    - `GET /api/dashboard/sessions`: List active/saved sessions.
    - `GET /api/dashboard/models`: List current model mappings and capabilities.
    - `POST /api/dashboard/models`: Create or update a model mapping.
    - `DELETE /api/dashboard/sessions/{id}`: Remove a session.
    - `PATCH /api/dashboard/config/transformers`: Update transformer toggles.
  </action>
  <verify>
    <automated>pytest tests/api/test_dashboard_api.py</automated>
  </verify>
  <done>Dashboard API provides required CRUD functionality.</done>
</task>

<task type="auto">
  <name>Task 2: Register Dashboard Router</name>
  <files>src/nvd_claude_proxy/app.py</files>
  <action>
    Include the `dashboard_router` in the main FastAPI `app` with the `/api/dashboard` prefix.
  </action>
  <verify>
    <automated>curl http://localhost:8788/api/dashboard/sessions</automated>
  </verify>
  <done>Dashboard API is accessible from the application.</done>
</task>

</tasks>

<success_criteria>
- Dashboard API is functional and covered by unit/integration tests.
- Models and sessions can be managed via HTTP requests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-session/02-03-SUMMARY.md`
</output>
