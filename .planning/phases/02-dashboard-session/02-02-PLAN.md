---
phase: 02-dashboard-session
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/nvd_claude_proxy/middleware/session.py, src/nvd_claude_proxy/app.py, src/nvd_claude_proxy/routes/messages.py]
autonomous: true
requirements: [SESS-02, SESS-03]

must_haves:
  truths:
    - "Requests with sk-ncp-* key are identified and assigned a session"
    - "ToolIdMap persists between requests for a given session key"
  artifacts:
    - path: "src/nvd_claude_proxy/middleware/session.py"
      provides: "FastAPI middleware for session handling"
---

<objective>
Implement the session isolation middleware to manage persistent ToolIdMap and TransformerChain states per API key.

Purpose: Enable stateful interactions and customization per user/application.
Output: Middleware and updated request handling logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@nvd-claude-proxy/.planning/phases/02-dashboard-session/02-01-SUMMARY.md
@nvd-claude-proxy/src/nvd_claude_proxy/routes/messages.py
@nvd-claude-proxy/src/nvd_claude_proxy/translators/tool_translator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SessionManager</name>
  <files>src/nvd_claude_proxy/db/session_manager.py</files>
  <action>
    - Implement methods to retrieve/save `ToolIdMap` state from `SessionRecord`.
    - Provide an async `get_or_create_session(api_key: str)` function.
    - Support serializing/deserializing `ToolIdMap` to JSON.
  </action>
  <verify>
    <automated>pytest tests/unit/test_session_manager.py</automated>
  </verify>
  <done>SessionManager correctly handles persistence of ToolIdMap.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SessionIsolationMiddleware</name>
  <files>src/nvd_claude_proxy/middleware/session.py, src/nvd_claude_proxy/app.py</files>
  <action>
    - Create middleware that extracts `X-NCP-Key` (or `Authorization: Bearer sk-ncp-*`) from headers.
    - Attach the session object to `request.state.session`.
    - Register the middleware in `app.py`.
  </action>
  <verify>
    <automated>curl -H "X-NCP-Key: sk-ncp-test" http://localhost:8788/health</automated>
  </verify>
  <done>Middleware correctly identifies sessions from request headers.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor messages route to use session</name>
  <files>src/nvd_claude_proxy/routes/messages.py</files>
  <action>
    - Update `create_message` to use `request.state.session.tool_id_map` instead of creating a new one.
    - Ensure `tool_id_map` is saved back to the DB after request completion (or as part of session cleanup).
  </action>
  <verify>
    <automated>pytest tests/e2e/test_persistent_sessions.py</automated>
  </verify>
  <done>Messages route preserves tool ID mapping across requests.</done>
</task>

</tasks>

<success_criteria>
- Tool ID mappings are persisted across independent HTTP requests using the same session key.
- Middleware is transparently integrated into the FastAPI application.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-session/02-02-SUMMARY.md`
</output>
